# LOL BR Loot

React and Firebase loot queue manager.

Copy `.env.example` to `.env.local` and fill in the Firebase web configuration before running locally.

The production Firebase web configuration is stored in `.env.production` and is included automatically in GitHub Pages builds.
LOLBR loot system
